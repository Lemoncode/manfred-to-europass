import { SwitchRoutes } from "./routes.model";

export const switchRoutes: SwitchRoutes = {
  root: "/",
  aboutUs: "/aboutUs",
  upload: "/upload",
  uploading: "/uploading",
  verify: "/verify",
  convert: "/convert",
  download: "/download",
};
